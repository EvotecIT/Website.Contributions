# Examples

Files in this folder are examples only. They are ignored by the contribution importer because the importer only reads post bundles from:

```text
posts/<language>/<year>/<slug>/index.md
```

Use these examples as copy-and-adjust references when creating a real submission under `posts/`.
